<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace = "com.smhrd.spofit.mapper.MemberMapper">
	
	<select id="findId" parameterType="com.smhrd.spofit.domain.Member">
		select m_id from t_member where m_email='${m_email}'
	</select>
  </mapper>